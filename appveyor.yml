version: 0.1.{build}

branches:
  only:
    - master

image: Visual Studio 2017
platform:
  - x64

cache:
  - node_modules
  - '%APPDATA%\npm-cache'
  - '%USERPROFILE%\.electron'
  - '%USERPROFILE%\AppData\Local\Yarn\cache'

init:
  - git config --global core.autocrlf input

install:
  - ps: Install-Product node 14 x64
  - git reset --hard HEAD
  - yarn config set ignore-engines true
  - npm install
  - npm install fs-extra@9.1.0
  - npm run build
  - node --version

build_script:
  - yarn build

test: off
environment:
  auth_token:
    secure: vCOhQjgNE62Xl3xZ9oeA++9llSpvrHR0o0PG1UULj2M0jk8C5EyTiu+0OglvT69K

deploy:
  release: gowe-v$(APPVEYOR_BUILD_VERSION)
  description: 'Release description'
  provider: GitHub
  auth_token:
    secure: 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
  artifact: build/.*\.exe
  draft: false
  prerelease: false
  on:
    branch: master
